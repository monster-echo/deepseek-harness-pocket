# React Native 客户端环境变量（复制为 .env 并填写）
# 三个必填，未配置则启动即报错退出（见 README「应用身份」）。

# 该 App 的租户 id（服务端按此隔离数据）
EXPO_PUBLIC_APP_ID=dshcompanion

# 运行环境：development / staging / production
EXPO_PUBLIC_APP_ENVIRONMENT=development

# 后端 API 基地址（掌鲸 DSH Pocket 统一认证）
EXPO_PUBLIC_API_URL=https://auth.zhongbei.tech

# Gateway 中转服务 WebSocket/API 基地址（外网主路径）
# 本地开发默认同机 gateway；生产为部署域名（wss 兼容，连接时自动处理）
EXPO_PUBLIC_GATEWAY_URL=ws://127.0.0.1:3781
