{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口与控制台窗口的能力集合。两个窗口都要授权：控制台是独立窗口，缺 core:event 就无法订阅 worker-status / console-panel 事件。",
  "windows": ["main", "console"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
